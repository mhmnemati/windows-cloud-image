# Cache objects
packer_cache/

# Crash log
crash.log

# Exclude all .pkvars.hcl files, which are likely to contain sentitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data points which are potentially sensitive and subject 
# to change depending on the environment.
#
*.pkvars.hcl

# For built boxes
*.box

# Input directories
input*

# Output directories
output*